export interface AuthRespones{
  "session_code"?: string;
}

export interface Usuario{
  firstName: string;
  firstLastName: string;
}

export interface Doctor{
  firstName:string;
  firstLastName: string;
}
